# HANDOFF - 2026-07-16 14:52

## 現在のタスク

- 承認済み「ダム前後の縦断描画修正・格子収束監査」を実装し、全TODO、独立監査、ブラウザ検証、`origin/main`反映まで完遂する。

## 目的と制約

- 入力ダム位置と実効計算面を明示し、黄色線・水面・移動床を同じfaceへ描く。
- 水面・移動床はダム面で段差表示し、固定床は連続表示する。平滑化で物理的不連続を隠さない。
- 31・61・121点で実効面、L1、左右状態、有限性、非負水深、CFL、保存を監査する。
- 単一HTML、700KiB以下、外部必須依存0、CSV schema 1、Worker protocol 1、384MiB上限を維持する。
- Workerはface/faceXを独立再計算し、クライアント派生値を信用しない。

## 進捗

- [x] 環境、git、MEMORY、旧HANDOFF、旧PLAN/SPEC/TODOを確認。
- [x] 前サイクルを`.spec/archive/2026-07-16-pre-dam-profile-rendering/`へ保存。
- [x] 新PLAN/SPEC/TODO/KNOWLEDGEを作成。
- [x] 独立read-only監査タスク`019f6979-cefb-7d90-96e2-a6253ff5b7d8`を作成。
- [ ] 計算面投影、段差パス、UI、Canvas、試験を実装。
- [ ] 収束監査、独立監査反映、ブラウザ検証、記録更新、commit/push。

## 次に最初にやること

- `resolveStructureFace`と`buildFaceAwareProfilePath`を純粋関数として追加し、Worker/mainの既存face投影を同じtie-break仕様へ統一する。
- 旧`drawProfile`のdam.x/最寄りセル基部を、実効faceXと左右河床最大値へ置換する。

## 注意点・ブロッカー

- 開始commitは`40cbb50`、開始時worktreeはclean。
- 実装前の基線は自己試験68/68、標準1200秒24,001 step・243 frame、HTML約310KB。
- 数値収束不合格時は描画だけで完了扱いにせず、ダム流束・河床更新を別SDDへ切り分ける。
- 現時点のブロッカーなし。
