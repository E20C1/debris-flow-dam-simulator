# HANDOFF - 2026-07-16 14:12

## 現在のタスク

- KANAko 1.44実機挙動との比較を踏まえた、単一HTMLの設定編集・状態整合性改善を完了した。

## 完了内容

- [x] 計算、検証、停止処理、ensemble、DEM、CSV読込中のUI・編集入口二重ロックとrun hash固定。
- [x] 不透過・スリット・格子の形式固有UI、安全既定値、モデル差表示、同一計算面ダム拒否。
- [x] ダム・観測点を中央に近い未使用計算面/計算点へ追加。
- [x] 流入ハイドログラフ表、行補間追加、最低2点・単調時刻・濃度上限の検証、流入/観測点重ね描画。
- [x] 河道計算点の線形再標本化、地点編集、堆積層厚全区間適用。
- [x] data URI faviconとCSV file inputのアクセシブル名を追加。
- [x] `.spec/TODO.md`全項目と`.spec/KNOWLEDGE.md`の証拠を更新。

## 最終証拠

- 統合自己試験: 68/68、fail 0。3形式ダムの数値応答、2点最小ケース、同一面拒否、再標本化、CSV往復を含む。
- 標準1200秒: 24,001 step、243 frame、表示時刻1,200秒、case hash `33b0381e`。
- 実行/ensemble中は材料・河道・流入・ダム・CSV・開始操作が無効。停止後は再有効化し、hashと入力値は不変。
- UI CSV往復: Manning 0.031、hash `58bc1f19`を保存し、0.04へ変更後の再読込で両方復元。
- 390×844: 設定・解析・診断の各状態で`scrollWidth = clientWidth = 390`。console error/warning 0。
- a11y: `lang=ja`、可視・有効な無名control 0、最初のTabはskip link、focus outlineあり。
- HTML: 約310KB、重複ID 0、inline script 3本の構文検査合格、外部必須resource 0。
- 検証用HTTPサーバーPID 37968を停止し、127.0.0.1:8765 LISTEN 0。Playwright CLI session 0。

## 試したことと結果

- KANAko 1.44を公開UIだけで操作し、形式別ダム項目、30～50点制限、地点・流入・堆積層・観測点、計算中ロックを確認した。コードや画像は流用していない。
- 内蔵ブラウザの`file://`新規遷移は安全ポリシーに拒否されたため迂回しなかった。従来のChrome `file://` 57/57基線、外部必須依存0、同一HTMLからのBlob Worker生成は維持されている。

## 次のセッションで最初にやること

- 追加依頼がなければ作業不要。新しい物理機能を始める場合は`.spec/`を新サイクルとしてアーカイブし、観測・実験データと校正基準を先に確定する。

## 注意点・ブロッカー

- 高度モードは縮約実験モデルでUSGS D-Claw互換ではなく、防災判断用途を主張しない。
- KANAko canonical出力は未公開で、数値一致はN/A。格子型パラメータの一対一換算も行わない。
- 今回の自動`file://`再試験だけはブラウザ安全ポリシー上未実施。未解消の実装ブロッカーはない。
