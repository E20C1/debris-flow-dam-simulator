# HANDOFF - 2026-07-15 22:49

## 使用ツール

- Codex Desktop / PowerShell / Git / Node.js / Python

## 現在のタスクと進捗

- [x] 実装計画と主要な仕様判断をユーザー承認済み。
- [x] 新規プロジェクト名、GitHubリポジトリ、`main`ブランチを確定。
- [x] make_project初期構築をcommit `e43c76c` として`origin/main`へpushする。
- [ ] `.spec/TODO.md` の実装項目を上から順に完了する。
- [ ] 別Codexタスク `019f6606-edf6-7fc0-8581-3697d75940a6` の独立監査を完了まで追跡し、結果を取り込む。

## 決定済み事項

- 成果物名: `debris_flow_dam_simulator.html`
- 可搬I/O: CSVのみ
- 画面: KANAkoの縦断図＋観測点グラフを現代化して踏襲
- 段階: 基盤 → 高精度1D → 1D–2D → 高度二相流・感度分析 → 総合監査
- 地形: 国土地理院DEM＋ローカルCSV
- 高度二相流は検証完了まで実験表示

## 次にやること

1. 初期ファイルを監査し、Git初期化・`origin/main` pushを完了する。
2. HTML骨格、CSSトークン、状態管理、CSV parser/serializer、Workerプロトコル、自己試験ランナーを実装する。
3. 各フェーズ完了時にTODO・KNOWLEDGE・HANDOFFを更新する。

## 注意点・ブロッカー

- 現在の親ディレクトリには無関係な既存HTMLがあるため、必ず新規サブフォルダ内だけを変更する。
- クライアントだけで完結するため「サーバーが正」は存在しない。代わりに、UIとは独立した正規化・検証層とWorker内再検証を二重化する。
- 独立監査タスクは、成果物が現れるまでコード合否を未証明としている。
