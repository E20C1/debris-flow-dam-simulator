# TODO - ダム前後の縦断描画修正・格子収束監査

## 0. 仕様・継続性

- [x] 前サイクルのPLAN、SPEC、TODO、KNOWLEDGEを`.spec/archive/2026-07-16-pre-dam-profile-rendering/`へ保存する。
- [x] ユーザー承認済み計画をPLAN/SPEC/TODOへ決定事項として反映する。
- [x] 独立read-only監査タスク`019f6979-cefb-7d90-96e2-a6253ff5b7d8`を作成する。
- [x] HANDOFFへ目的、制約、進捗、次の操作、独立監査IDを記録する。

## 1. 計算面コンポーネント

- [x] `resolveStructureFaces`と単数wrapperを純粋関数として実装し、一括二分探索へ既存投影箇所を統一する。
- [x] Workerがface/faceXを独立再計算し、クライアント派生値を信用しないことを維持する。
- [x] 通常格子、非一様格子、極大・微小座標、完全一致、局所距離ULP同距離上流tie-break、表現不能面、無効入力を試験する。

## 2. ダムUI・縦断描画

- [x] ダム詳細へ入力位置、実効計算面、差を表示する。
- [x] `buildFaceAwareProfilePath`を実装し、水面・移動床だけをダム面で段差化する。
- [x] ダム線を実効面へ移し、基部・天端をWorkerのcrest定義へ一致させる。
- [x] 描画注記をDOM・Canvas名へ追加し、高天端をy-rangeへ含め、平滑化や表示専用クランプを導入しない。
- [x] パス生成、複数ダム、入力不変、ダムなし回帰、基部・天端を単体試験する。

## 3. 格子収束監査

- [x] 31・61・121点の実効面誤差5→2.5→1.25mを試験する。
- [x] 確率閉塞を無効化した31・61・121点で1200秒計算を実行し、0・900・1200秒frameを収集する。
- [x] 固定物理座標・固定10m除外・区分一定control-volume重なりによるL1指標を実装する。
- [x] 水面・水深・河床変動のL1と収束次数を再判定し、同一支持領域の水深解像度適合29.9%/67.8%未達と左右trace未達を制約として記録する。
- [x] 有限性、非負水深、CFL、保存診断、Worker応答境界を確認する。

## 4. 独立監査

- [x] 別タスクの初回監査が完了するまで状態を確認する。
- [x] 初回P0/P1/P2指摘を現行コードと照合し、実測値付きの再監査を依頼する。
- [x] P0/P1をすべて修正・再検証し、監査タスクから「P0/P1なし・実装完了可」の最終判定を得る。

## 5. 実ブラウザ・回帰

- [x] 全自己試験を実ブラウザで75/75合格させる。
- [x] 0・900・1200秒でダム線と段差のpixel位置、斜めスパイク解消、ラベルを目視確認する。
- [x] リセット、タイムライン、3形式ダム、CSV往復を回帰確認する。
- [x] console、a11y、390px、外部resource、HTMLサイズ、重複ID、構文を確認する。

## 6. 記録・反映

- [x] KNOWLEDGEへ原因、実装、収束値、意図的制約、監査結果を記録する。
- [x] MEMORYをアーカイブして最新基線を記録する。
- [x] HANDOFFを完了証拠付きで更新する。
- [x] HTTPサーバー、Playwright、不要な補助プロセスを停止する。
- [ ] commit、`origin/main` push、clean、`main == origin/main`を確認する。
